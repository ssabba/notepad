import { createTheme } from "@mui/material";

export const Card = {
  styleOverrides: {
    // Name of the slot
    root: {
      borderRadius: "14px",
      background: "#F5F5F7",
      boxShadow: "none",
    },
  },
};
